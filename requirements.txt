# FIFA World Cup 2026 Forecast — Python dependencies
# All-Python stack, fewest moving parts (architecture §3.1).

# Data + numerics
pandas>=2.0
numpy>=1.24
scipy>=1.10
statsmodels>=0.14

# Plotting (Step 5 reliability diagram; static PNG via the Agg backend)
matplotlib>=3.5

# Serving layer (Step 7: FastAPI API + Streamlit dashboard)
fastapi>=0.110
uvicorn>=0.27
streamlit>=1.30

# HTTP for data fetching
requests>=2.31

# Tests
pytest>=8.0
httpx>=0.24            # FastAPI TestClient (Step 7 endpoint tests)
